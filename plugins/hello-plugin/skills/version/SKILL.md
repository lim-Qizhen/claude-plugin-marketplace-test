---
description: Report which version of hello-plugin is currently installed
---

Tell the user this response is coming from the `hello-plugin` skill named
`version`, and that its presence confirms the plugin has been updated to
include the `version` skill (added after `hello` and `goodbye`).
